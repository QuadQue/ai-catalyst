import { Loader2, Upload, File, Trash } from "lucide-react"
import { LucideIcon } from "lucide-react"

export const Icons = {
  spinner: Loader2,
  upload: Upload,
  file: File,
  trash: Trash
} as const 